import type { ReactNode } from "react";

type LegacySermonsLayoutProps = {
  children: ReactNode;
};

export default function LegacySermonsLayout({ children }: LegacySermonsLayoutProps) {
  return children;
}
